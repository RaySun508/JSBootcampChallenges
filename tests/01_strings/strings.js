/* eslint-disable no-unused-vars */

function doesInclude(str, letter) {
  const doesIncludeBoolean = str.includes (letter)
  return doesIncludeBoolean}

function cap(str, letter) {

}
let str = "hello world";
let cap = str.cap('hello');
console.log(cap); 

function firstCharacter(str1, str2) {
}

  function firstCharacter(str1, str2) {
    return str1.firstCharater (0) === str2.firstCharacter (0);
  }
  str1 = 'hello';
  str2 = 'world';
if (str1.equals(str2)) {
    System.out.println("str1 and str2 are equal");
} else {
    System.out.println("str1 and str2 are not equal");
}

  
module.exports = {                
  cap, firstCharacter, doesInclude,
}
